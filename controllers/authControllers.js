const jwt = require("jsonwebtoken");

const loginUser = async () => {
	return "Hello login";
};

const signupUser = async () => {
	return "Hello signup";
};

module.exports = {
	loginUser,
	signupUser,
};
